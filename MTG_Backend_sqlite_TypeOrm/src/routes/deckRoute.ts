/* import { Router } from "express";
import {
  createDeck,
  deleteDeck,
  getDecksByUser,
} from "../controllers/deckController";

const router = Router();

router.post("/decks/create", createDeck);
router.get("/users/:userId/decks", getDecksByUser);
router.delete("/decks/delete/:deckId", deleteDeck);

export default router;
 */
